// Illustratorで位置キープで文字揃え方向を変えるやつ// 2016.05.18  by（z-）function main () {    if (! app.documents.length) return;    var sel = app.activeDocument.selection;    //if (! sel.length) return; // 不要でしたな        var obj, top, left;    while (sel.length) { // いちおうよくばりループ        obj = sel.shift ();        if (sel.reflect.name != "TextFrame") continue; //ここ未検証てきとう        top = sel.top;        left = sel.left;        changeJustification (obj, 0); //これから実装する関数、パラメ2ndはまだてきとう        sel.top = top;        sel.left = left;        }    }function changeJustification (obj, val) { // あとでかく    //objの揃え方向をvalに    }main ();